server:
  port: 8000  #eureka注册中心服务器端口为8001

spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chouxin/spring-cloud-config-repo/    #项目名称
          search-paths: config-repo         #资源文件 文件夹
          username: 342175867@qq.com
          password: xxxx
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/